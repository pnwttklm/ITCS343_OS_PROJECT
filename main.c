#include <stdio.h>
#include <string.h>
#define B 300;
#define R 100;

int main(){
	char word[B];
	while(1){
		if(scanf(" %s", word) != EOF){
			
			printf("word: %s\n", word);
}else{break;}
}
return 0;
}
